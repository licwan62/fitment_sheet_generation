本文件是一次全量表更新任务。非皮卡车的全量表格式和回复格式如下：
主车型	品牌	分类	结构	版本	代际	年份	max_length_in	max_width_in (w/o)	max_height_in	参考车型	备注	迭代状态

如果列表或者输入全量表里有皮卡车数据，则必须使用皮卡车的全量表格式和回复格式,如下：
主车型	品牌	分类	子车系	版本	代际	年份	驾驶室类型	货斗长度_ft	max_length_in	max_width_in (w/o)	max_height_in	参考车型	备注	状态

ChatGPT 要求：
本任务和图片生成无关，请不要生成图片。

分析要求：
1. 皮卡和非皮卡记录分开处理，最终输出两个格式不同的全量表，并且在回复末尾分别输出“非皮卡车全量表完成”和“皮卡车全量表完成”
2. 请只处理本文件中的车型数据，不要引用其他批次。
3. 输出 TSV。
4. 不要漏行。
5. 字段顺序必须保持。
6. 每一轮回复都必须输出一个更新过的 TSV；不得只描述这一轮打算做什么、不得只给计划、不得只给解释而不附 TSV。
7. 每一轮输出的 TSV 都必须使用同一字段顺序，不要新增、删除、改名字段。
8. 中间轮次允许输出阶段性 TSV，但阶段性 TSV 也必须是真正更新过的 TSV，不能只写“后面再整理”“下一轮再给表”。
9. 如果还有记录没有数据、仍需核对补强、仍有待终核、或只输出了部分车型/部分年份/部分结构，则必须在回复末尾输出：下一步。
10. 每一轮未完成回复都必须显式包含以下推进信号，且顺序建议固定：
   更新点
   当前批次进度
   本轮更新后的 TSV
   下一步优先核对
   下一步
11. 未完成时，禁止连续两轮只重复相同说明而没有新增 TSV 改动、状态推进、年份补强、结构拆分或参考车型覆盖扩展。
12. 为避免超时，除必要的更新点/进度/下一步说明外，不要输出长篇解释；把主要篇幅留给 TSV。
13. 非皮卡车中，如果“版本”列包含门数信息，例如 `2dr`、`4dr`、`2-door`、`4-door`、`两门`、`四门` 或其他门数口径，必须把不同门数作为独立记录拆开推进，不能把不同门数合并在同一条记录里。
14. 当同一车型/代际/年份段同时存在多个门数版本时，`版本` 列必须明确保留对应门数口径；如果某行同时写了 `2dr/4dr`、`2-door / 4-door` 这类混合信息，必须拆成多行分别处理，并分别给出各自参考车型、备注和迭代状态。

年份与参考车型规则：
1. 如果“年份”列是单年，例如 2002，则“参考车型”可以写 2002 Audi A6 Avant。
2. 如果“年份”列是连续范围，例如 2002-2004，则“参考车型”必须体现整个范围已经核对，例如：
   2002-2004 Audi A6 Avant
   或 2002/2003/2004 Audi A6 Avant
   或 2002/2004 Audi A6 Avant（仅当确认中间年份同款同尺寸）
3. 不能把 2002-2004 的“参考车型”只写成 2002 Audi A6 Avant；这代表 2003/2004 没有被核对完，必须保留待补强并继续输出：下一步。
4. 如果同一行覆盖多个年份、多个结构、多个版本，参考车型必须覆盖该行年份范围和结构/版本口径；否则该行不能标为可入库。
5. 如果当前不是最后一轮，也必须输出本轮更新后的 TSV，不能只回复说明文字。

最终结束规则（必须严格遵守）：
1. 最后一轮必须输出当前文件中本批次更新后的完整可替换 TSV。
2. 这个完整 TSV 必须包含未变更数据、已修改数据、新增/拆分数据在内的当前批次全部记录。
3. 不能只输出“所有可入库”“全部可入库”“可出库”“已完成”等总结句后结束。
4. 不能只输出变化部分、部分车型、部分品牌、部分年份或摘要表后结束。
5. 如果你想表达“所有可入库/可出库”，必须先给出完整可替换 TSV，再在完整 TSV 后单独输出一句：本批次完成。
6. 最终回复顺序必须是：
   更新点（如有）
   当前批次更新后的完整可替换 TSV
   本批次完成

如果我发送“给我当前完整全量表”，你必须只输出当前批次更新后的完整可替换 TSV，并在表格后输出：本批次完成。不要输出局部表、摘要或解释。
