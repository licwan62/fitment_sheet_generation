本任务是全量表补强，每轮答复必须带TSV。

核心要求：
- 只处理本文件数据，不要引用其他批次。
- 输出 TSV，字段顺序必须保持，不增删改名。
- 不漏行，每轮必须给出更新过的 TSV，不能只写计划或说明。
- 阶段性 TSV 也必须是真正更新后的表，不能只写“后面再整理”。
- 如果仍有待补强、待终核、部分车型/年份/结构未完成，则回复末尾写“下一步”。
- 未完成时建议固定顺序：更新点 / 当前批次进度 / 本轮更新后的 TSV / 下一步优先核对 / 下一步。
- 不能连续两轮只重复说明而无实际 TSV 变化。
- 非皮卡中若 `版本` 含门数信息（如 2dr、4dr、2-door、4-door、两门、四门），必须拆成独立记录。
- 同一车型/代际/年份段多个门数版本时，`版本` 需保留具体门数；混合写法如 `2dr/4dr` 必须拆行。

字段填写规则：
1. 普通车和皮卡统一使用同一套字段，不再分两套表头，字段如下：

主车型	品牌	分类	子车系	结构	版本	代际	年份	驾驶室类型	货斗长度_ft	max_length_in	max_width_in (w/o)	max_height_in	参考车型	备注	迭代状态

2. 普通车填写规则：
- 分类：只填 三厢车 / 两厢车 / 跑车 / 越野车
- 子车系：普通车一般留空
- 结构：填写单一车身结构，例如 Sedan / Coupe / Convertible / Wagon / Hatchback / SUV / CUV / Roadster / Targa
- 版本：无特殊版本时填 Std.；有特殊版本时填 Type S / A-Spec / Hybrid / EV / PHEV 等
- 驾驶室类型：留空
- 货斗长度_ft：留空

3. 皮卡填写规则：
- 分类：固定填 皮卡
- 子车系：填写具体子车系，例如 F-150 / F-250 / F-350 / Silverado 1500 / 2500HD / 3500HD / Tacoma / Tundra
- 结构：固定填 Pickup
- 版本：普通版填 Std.；特殊版填 Raptor / Tremor / Lightning / DRW / TRD Pro / ZR2 / AT4X / Rebel / Power Wagon / Rubicon/Mojave 等
- 驾驶室类型：填写 Regular Cab / Extended Cab / SuperCab / Double Cab / Crew Cab / SuperCrew / Access Cab / Quad Cab 等
- 货斗长度_ft：填写数字，例如 5.0 / 5.5 / 6.0 / 6.5 / 6.75 / 8.0

4. 皮卡的结构字段不要写 Short Bed / Standard Bed / Long Bed / Crew Cab Short Bed 等内容。
这些信息必须拆到“驾驶室类型”和“货斗长度_ft”两个字段里。
皮卡结构字段统一只写 Pickup，避免信息重复。

5. 版本字段不要留空：
- 普通车型无特殊版本填 Std.
- 皮卡无特殊版本填 Std.

核对规则：
- 年份单年可写对应参考车型，如 `2002 Audi A6 Avant`。
- 年份范围必须用参考车型体现整段核对，如 `2002-2004 Audi A6 Avant`、`2002/2003/2004 Audi A6 Avant`，或仅在确认全部年份同款时写 `2002/2004 Audi A6 Avant`。
- 不能把 `2002-2004` 只写成 `2002 Audi A6 Avant`，否则必须继续补强并输出“下一步”。
- 覆盖多个年份/结构/版本的行，参考车型必须对应全部覆盖，否则不能标可入库。

当前批次范围规则：
- 只处理当前 TSV 文件内已有记录所覆盖的车型、代际、结构、版本和年份范围。
- 不要为了追求某个车型从首代到末代完整覆盖而新增当前 TSV 范围外的年代、代际或车型行。
- 如果同一车型在其他 split 文件中还有更早或更晚的代际/年份，不要在当前 split 中补入，避免合并时重复。
- 当前批次输出顺序必须以输入 TSV 的第一条到最后一条为边界；例如输入第一条是 `2012-2018 BMW 3 Series Sedan`，最终结果也应从该记录或其拆分记录开始，不能新增更早的 `BMW 3 Series` 记录排到前面。
- 允许在当前记录自身年份范围内拆分年份、结构、门数、驾驶室或货斗；拆分后的年份合集不得超出原记录年份范围。
- 允许修正当前记录的代际命名、结构、版本、尺寸和参考车型；若发现输入年份范围本身明显错误，可以缩小或拆分并在备注/迭代状态说明，但不要扩展到当前行原年份范围之外。
- 若当前 TSV 内相邻记录之间看似缺少中间年份或代际，只有当缺失部分也属于当前 TSV 原有记录的年份范围时才补；否则保持不新增，并在需要时写“待终核: 当前 split 外年份不处理”。

迭代状态规则：
- 若参考车型覆盖整个年份区间，则写“可入库”。
- 若只覆盖部分年份，则写“待终核: 缺失年份范围”。
- 已有非年份缺项说明时，保留原信息，不被年份规则覆盖。

参考车型年份可以压缩：
- 连续年份可写成 `1986-1991`。
- 不连续年份保持斜杠分隔。
- 不同车型/结构/版本不得混合压缩。

结束规则：
- 最后一轮必须输出当前批次完整可替换 TSV，不能只写总结。
- 完整 TSV 需包含未变更、已改、拆分、新增等全部当前记录。
- 若要表达“全部可入库/可出库”，先给完整 TSV，再单独写“本批次完成”。
- 最终回复顺序：更新点（如有） / 当前批次更新后的完整可替换 TSV / 本批次完成。

“给我当前完整全量表”时，只输出当前完整可替换 TSV，之后写“本批次完成”。
