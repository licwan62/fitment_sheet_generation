# qclaw 全量表补强自动化实现文档

## 1. 任务目标

自动遍历输入目录中的 TSV 文件，将每个 TSV 文件与 requirement.md 一起发送到当前已登录的 ChatGPT 桌面端，等待回复完成后保存结果。

每个 TSV 文件可能需要多轮交互：

```text
发送 requirement + TSV
等待回复
如果未完成 → 发送“下一步”
等待回复
继续判断
直到出现批次完成关键词
然后进入下一个 TSV
```

---

## 2. 目录配置

```text
输入目录：
D:\Home\Scripts\fitment_sheet_generation\input_sheets\1_split_by_brand_50

输出目录：
D:\Home\Scripts\fitment_sheet_generation\output_sheets

日志文件：
D:\Home\Scripts\fitment_sheet_generation\log.csv

汇总文件：
D:\Home\Scripts\fitment_sheet_generation\summary.txt

要求文件：
D:\Home\Scripts\fitment_sheet_generation\requirement.md
```

---

## 3. 启动前检查

执行前必须检查：

```text
1. 输入目录是否存在
2. 输出目录是否存在，不存在则创建
3. requirement.md 是否存在
4. log.csv 是否存在，不存在则创建表头
5. summary.txt 可在全部任务结束后生成或覆盖
```

log.csv 表头：

```csv
文件名,开始时间,结束时间,状态,发送次数,输出文件名,备注
```

---

## 4. 文件遍历规则

```text
1. 只处理输入目录中的 .tsv 文件。
2. 按文件名升序处理。
3. 不删除 input 文件。
4. 不覆盖已有 output 文件。
5. 如果输出文件已存在，则自动加后缀：
   原文件名_result.md
   原文件名_result_2.md
   原文件名_result_3.md
```

建议读取 log.csv：

```text
如果某文件已记录为：
成功
重复终止
次数上限终止
页面错误
登录失效
偏离终止

则本次跳过，避免重复处理。
```

---

## 5. 每个 TSV 的首次发送格式

每个文件第一次发送到 ChatGPT 的内容必须固定为：

```text
【任务要求】
<requirement.md 完整内容>

【当前文件名】
<当前 tsv 文件名>

【TSV 数据】
<当前 tsv 完整内容>
```

目的：

```text
1. 避免 ChatGPT 混淆当前处理文件。
2. 让回复可追溯。
3. 降低跨批次偏移概率。
```

---

## 6. 多轮交互状态机

每个文件进入独立循环。

伪代码：

```text
for file in tsv_files:

    round = 1
    next_count = 0

    发送 requirement + 当前 TSV

    while True:

        等待 ChatGPT 回复完成

        复制本轮完整回复

        保存 / 追加到当前结果文件

        如果命中完成关键词:
            状态 = 成功
            break

        如果页面异常:
            状态 = 页面错误 / 登录失效
            break

        如果回复高度重复:
            状态 = 重复终止
            break

        如果 next_count >= 最大下一步次数:
            状态 = 次数上限终止
            break

        如果回复明显偏离当前文件任务:
            状态 = 偏离终止
            break

        发送：
        下一步

        next_count += 1
        round += 1
```

---

## 7. 回复完成判定

不能只靠时间等待。

必须同时满足：

```text
1. 页面不存在“停止生成”按钮
2. 输入框重新可编辑
3. 最近 10 秒回复内容没有变化
4. 页面没有明显报错
```

满足后再额外等待 2 秒，然后复制回复。

---

## 8. 结果保存规则

同一个 TSV 文件的多轮回复必须保存到同一个结果文件中。

格式：

```text
# 文件名：Acura.tsv

--- Round 1 / 首次发送 ---
<第 1 轮完整回复>

--- Round 2 / 下一步 ---
<第 2 轮完整回复>

--- Round 3 / 下一步 ---
<第 3 轮完整回复>
```

这样即使中途失败，也能保留已生成内容。

---

## 9. 完成关键词

如果任意一轮回复包含以下任一关键词，则判定当前 TSV 成功完成：

```text
本批次完成
已完成
当前批次完成
无待核
可入库全量表
全部可入库
全部完成
批次完成
批次结束
本轮结束
```

命中后：

```text
1. 保存当前回复
2. log.csv 记录状态：成功
3. 进入下一个 TSV 文件
```

---

## 10. 正常推进关键词

以下内容表示任务仍在正常推进，不应判定为偏离：

```text
更新点
当前批次进度
下一步优先核对
待终核
可入库
数据抓取过程
全量表
TSV
新增/拆出记录
主要数值修改
🟢
🟡
🔴
```

如果出现这些内容，但没有完成关键词，则继续发送：

```text
下一步
```

---

## 11. 下一步次数限制

建议默认：

```text
最大下一步次数：30
```

原因：

```text
全量表补强任务中，一个品牌 TSV 可能包含几十条车型。
真实核对常常需要多轮。
8 次容易误终止。
```

如果某个 TSV 特别大，可以手动提高到：

```text
50
```

---

## 12. 重复终止规则

不要因为连续多轮都有“下一步”或表格结构相似就终止。

只有同时满足以下条件才判定重复：

```text
1. 连续两轮回复相似度 > 95%
2. 新增有效字符 < 100
3. 没有新增车型、年份、状态、TSV 行
4. 没有出现新的“更新点”
```

判定重复后：

```text
状态 = 重复终止
备注 = 连续两轮回复高度相似，疑似未继续推进
```

---

## 13. 偏离终止规则

如果回复出现以下情况，判定为偏离终止：

```text
1. 明显处理了非当前 TSV 文件中的车型
2. 输出与 fitment / 全量表补强无关
3. 忽略 requirement.md 字段结构
4. 长篇解释但没有 TSV、更新点、当前进度或下一步方向
5. 明显开始回答其他历史对话任务
```

偏离终止后：

```text
1. 保存当前回复
2. log.csv 记录：偏离终止
3. 进入下一个 TSV
```

---

## 14. 页面错误与登录失效

出现以下情况，停止当前文件：

```text
页面错误
网络错误
ChatGPT 报错
输入框不可用
登录失效
长时间无响应
无法复制回复
```

状态记录：

```text
页面错误
或
登录失效
```

不要继续乱点。

---

## 15. 操作节奏限制

为了避免误操作：

```text
1. 每次点击、粘贴、发送之间至少等待 2 秒。
2. 粘贴大文本后等待 1-2 秒再发送。
3. 回复完成后额外等待 2 秒再复制。
4. 发送“下一步”前再次确认页面已停止生成。
5. 不操作微信、邮箱、网盘或其他软件。
```

---

## 16. 日志状态

状态只能使用：

```text
成功
重复终止
次数上限终止
页面错误
登录失效
偏离终止
```

每个文件处理结束后追加一行：

```csv
文件名,开始时间,结束时间,状态,发送次数,输出文件名,备注
```

发送次数定义：

```text
首次发送 + 下一步次数
```

---

## 17. summary.txt 内容

全部 TSV 处理完成后生成：

```text
总文件数：
成功数：
重复终止数：
次数上限终止数：
页面错误数：
登录失效数：
偏离终止数：
失败数：
输出目录：
完成时间：
```

失败数计算：

```text
失败数 = 重复终止数 + 次数上限终止数 + 页面错误数 + 登录失效数 + 偏离终止数
```

---

## 18. 推荐执行逻辑简版

```text
启动
↓
检查目录和文件
↓
读取 tsv 文件列表
↓
按文件名升序处理
↓
发送 requirement + 当前 TSV
↓
等待回复完成
↓
保存回复
↓
判断是否完成
    是 → 记录成功 → 下一个 TSV
    否 → 判断异常 / 重复 / 偏离 / 次数上限
        有异常 → 记录状态 → 下一个 TSV
        无异常 → 发送“下一步”
↓
直到所有 TSV 结束
↓
生成 summary.txt
```

---

## 19. 针对全量表补强的关键优化

必须保留以下策略：

```text
1. 一个 TSV 文件允许多轮“下一步”。
2. 只有出现完成关键词才进入下一个 TSV。
3. 中间出现“更新点 / 当前批次进度 / 待终核”属于正常推进。
4. 每轮都追加保存，避免失败后丢失。
5. 不要因为没有一次性完成就终止。
6. 不要把未出现完成关键词的文件记为成功。
7. 不要覆盖已有输出。
8. 不要删除输入文件。
```

---

## 20. 建议参数

```text
最大下一步次数：30
回复稳定检测时间：10 秒
操作间隔：2 秒
回复完成后额外等待：2 秒
重复相似度阈值：95%
最小新增有效字符：100
```
